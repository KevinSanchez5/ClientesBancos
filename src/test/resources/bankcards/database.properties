#SQLite en memoria
db.url=jdbc:sqlite::memory:

# Configuración Hikari
db.username=
db.password=
db.pool.size=10
db.connectionTimeout=30000
db.idleTimeout=600000
db.maxLifetime=90000

# Configuración base de datos remota
cards.database.url=jdbc:postgresql://postgres-db:5432/cards?user=postgres&password=password